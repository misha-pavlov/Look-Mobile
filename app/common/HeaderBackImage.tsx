import React from 'react';
import { Ionicons } from '@expo/vector-icons';
import { colors } from '../config/colors';

export const HeaderBackImage = <Ionicons name="ios-chevron-back" size={30} color={colors.white} />;
