import styled from 'styled-components/native';
import { colors } from '../../config/colors';

export const ScreenHeaderText = styled.Text`
  color: ${colors.white};
  font-size: 24px;
  font-weight: 600;
  padding-bottom: 25px;
  padding-top: 55px;
`;
