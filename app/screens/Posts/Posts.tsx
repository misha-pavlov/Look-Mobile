import React from 'react';
import { View, Text } from 'react-native';

const Posts = () => {
  // TODO: add check const { isUserAuthorized } = useIsUserAuthorized();
  return (
    <View>
      <Text>Posts</Text>
    </View>
  );
};

export default Posts;
