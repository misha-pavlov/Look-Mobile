import styled from 'styled-components/native';
import { colors } from '../../config/colors';

export const UserPostsListContainer = styled.View`
  background-color: ${colors.black};
  padding: 0 15px;
  flex: 1;
`;
